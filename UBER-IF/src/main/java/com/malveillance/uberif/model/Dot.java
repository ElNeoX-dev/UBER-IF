package com.malveillance.uberif.model;

import com.malveillance.uberif.model.Courier;
import com.malveillance.uberif.model.Intersection;
import javafx.scene.paint.Paint;
import javafx.scene.shape.Circle;

/*
public class Dot extends Intersection {
    private Circle circle;
    public Dot(Intersection intersection, double centerX, double centerY, double radius, Paint fill) {
        super(intersection.getId(), intersection.getLatitude(), intersection.getLongitude());
        this.circle = new Circle(centerX, centerY, radius, fill);

    }

    public Circle getCircle() {
        return circle;
    }


}*/
